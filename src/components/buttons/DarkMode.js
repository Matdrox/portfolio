import { motion } from 'framer-motion';

const DarkMode = () => {
  return (
    <motion.div className='bg-white w-30 h-30 rounded absolute top-0 right-0 m-10'>
      FWOPKEFW
    </motion.div>
  );
};

export default DarkMode;
